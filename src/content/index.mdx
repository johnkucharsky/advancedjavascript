# Snippets from Real Interviews

These JavaScript snippets are drawn from real interview questions and real tasks from my job designed to give you hands-on experience with advanced concepts.

I’m a frontend developer who went through about 20 interviews — and I gathered everything companies were asking me.
Each snippet is based on those actual questions.

You can copy any snippet, execute it, and step through it with a debugger to truly understand what’s happening.

What will this print?

```js filename="Promise Resolution Order" copy
const promise1 = Promise.resolve();
const promise2 = Promise.resolve();

promise1.then(() => console.log(1)).then(() => console.log(2));
promise2.then(() => console.log(3)).then(() => console.log(4));
```

And how about this?

```js filename="Event Loop" copy
console.log("1");

setTimeout(function () {
  console.log("2");

  Promise.resolve().then(function () {
    console.log("3");
  });
}, 0);

Promise.resolve().then(function () {
  console.log("4");

  setTimeout(function () {
    console.log("5");
  }, 0);
});

requestAnimationFrame(function () {
  console.log("7");
});

console.log("6");
```

[Event Loop](/learn-js-and-mdn/event-loop)

Let’s dive in 👇